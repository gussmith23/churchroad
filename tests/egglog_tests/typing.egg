; Handled by calling churchroad::import_churchroad(&mut egraph) in the macro.
; (include "egglog_src/churchroad.egg")

(let clk (Var "clk" 1))
(let a (Var "a" 8))
(let b (Var "b" 8))
(let simple
 (Op1 (Reg 0)
  (Op2 (And) a b)))

(run typing 10)
(check (HasType a (Bitvector 8)))
(check (HasType b (Bitvector 8)))
(check (HasType simple (Bitvector 8)))